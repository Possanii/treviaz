{
  "name": "treviaz_app",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "cd apps/api && pnpm format && cd ../web && pnpm format"
  },
  "devDependencies": {
    "turbo": "^2.1.1"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@keycloak/keycloak-admin-client": "^26.1.2",
    "keycloak-connect": "^26.1.1"
  }
}
